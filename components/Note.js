import React, { Component } from 'react';
import { View } from 'react-native';

export default class Note extends Component {
  render() {
    return (
      <View>
        // code for editable textbox here
      </View>
    );
  }
}
