import React, { Component } from 'react';
import { View } from 'react-native';

export default class Weeks extends Component {
  render() {
    return (
      <View>
        // conditional render weeks grid (default to full life view)
      </View>
    );
  }
}
